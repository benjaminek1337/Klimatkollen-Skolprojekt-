@model IEnumerable<float>
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>

@*@{var myLabels = Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(x => x).ToList());
    var myData = Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(x => x).ToList()); }*@
<h1>Default</h1>

<script type="text/javascript">
    google.charts.load('current', { 'packages': ['line'] });
    google.charts.setOnLoadCallback(drawChart);
    function drawChart() {

        var data = new google.visualization.DataTable();
        data.addColumn('number', 'Datum');
        data.addColumn('number', 'Temperatur');

        
            //Få in dina siffror på båda? For-loop?
           @*data.addRows([ @Html.Raw(Model.ToString())*@

        //        [1,  41.8],
        //    [2, 32.4],
        //    [3, 25.7],
        //    [4, 10.5],
        //    [5, 10.4],
        //    [6, 7.7],
        //    [7, 9.6],
        //    [8, 10.6],
        //    [9, 14.8],
        //    [10, 11.6],
        //    [11, 4.7],
        //    [12, 5.2],
        //    [13, 3.6],
        //    [14, 3.4]
        ]);

        var options = {
            chart: {
                title: 'Box Office Earnings in First Two Weeks of Opening',
                subtitle: 'in millions of dollars (USD)'
            },
            width: 900,
            height: 500,
            axes: {
                x: {
                    0: { side: 'top' }
                }
            }
        };

        var chart = new google.charts.Line(document.getElementById('line_top_x'));

        chart.draw(data, google.charts.Line.convertOptions(options));
    }
</script>

@foreach (var v in Model)
{
    <li>@v.ToString()</li>
}


<div id="line_top_x"></div>
@*//Test*@